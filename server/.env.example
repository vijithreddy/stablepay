# Server Configuration
PORT=3000

# CDP (Coinbase Developer Platform) Credentials
CDP_API_KEY_ID=your-cdp-api-key-id
CDP_API_KEY_SECRET=your-cdp-api-key-secret

# Twilio Credentials
TWILIO_ACCOUNT_SID=your-twilio-account-sid
TWILIO_AUTH_TOKEN=your-twilio-auth-token
TWILIO_VERIFY_SERVICE_SID=your-twilio-verify-service-sid

# Webhook Security
# Get this from Coinbase Developer Portal when creating webhook
# The signing secret is provided in the webhook creation response
WEBHOOK_SECRET=your-webhook-signing-secret

# Redis Configuration (for push notification storage in production)
# Set up at: https://vercel.com/dashboard/stores (Vercel Marketplace)
# Automatically provided by Vercel when you create a Redis store
# Leave empty for local dev (uses in-memory storage)
REDIS_URL=your-redis-url

# APNs Configuration (for iOS push notifications)
# Get from: https://developer.apple.com/account/resources/authkeys/list
# Leave empty to fallback to Expo push service
APNS_KEY_ID=your-apns-key-id
APNS_TEAM_ID=your-apns-team-id
# Paste your .p8 file content as a single line (replace newlines with \n)
APNS_KEY=-----BEGIN PRIVATE KEY-----\nYOUR_KEY_HERE\n-----END PRIVATE KEY-----